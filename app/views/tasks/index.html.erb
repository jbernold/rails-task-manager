<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <p><%= link_to task.title, task_path(task) %> <%= link_to "Delete this task", task_path(task), method: "delete", data: { confirm: "ARE U SURE" } %> </i></p>
<% end %>

  <p><strong><%= link_to "+ Add a new task", new_task_path %></strong></p>
